- className: "com.hortonworks.registries.auth.server.AuthenticationFilter"
  params:
    type: "kerberos"
    kerberos.principal: "${HWX_REGISTRY_HTTP_PRINCIPAL}"
    kerberos.keytab: "${HWX_REGISTRY_KEYTAB}"
    kerberos.name.rules: "${HWX_REGISTRY_AUTH_KRB_RULES}"
